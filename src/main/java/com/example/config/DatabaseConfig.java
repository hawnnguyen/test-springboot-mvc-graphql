package com.example.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class DatabaseConfig {
    // Spring Boot will handle database initialization automatically
    // based on our application.yml configuration
}
